// import getRequestConfig from './locale';

import type { Locale } from "./locale";
export type { Locale };

import { locales, defaultLocale, timeZone } from "./locale";
export { locales, defaultLocale, timeZone };

import { themes, CodisThemeProps } from './themes';
export { themes, CodisThemeProps }

export const appName = 'codis';

export * from './auth';